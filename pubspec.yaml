name: secret_calculator
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  math_expressions: ^2.3.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  fluttertoast: ^8.0.9
  permission_handler: ^10.0.2
  photo_manager: ^2.3.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: ^1.1.3
  build_runner: ^2.2.1

flutter:
  
  uses-material-design: true
  
  assets:
    - assets/
  
